
###########################    Common Inputs    ###########################
[common]
mode = "standalone"

# Modules to run (requires module input sections below)
models_to_run=['electricity']

# Temporal resolution (inter-annual) 
# default = 16 representative (rep) days and 4 rep hours  
# d8h12 = 8 rep days and 12 rep hours,  d4h24 = 4 rep days and 24 rep hours, or custom  
temporal_resolution = "default"

# path to data that may be common-access to several sub-models
common_data_path='input/common'

# Temporal resolution (annual)
# list format, cannot be less than or greater than input data
aggregate_years=true
aggregate_start_year = 2023 # initial year to start aggregation from, iff aggregate_years is enabled, else ignored
summary_years = [2025, 2030] # evaluation years / aggregation summary years (if selected)

###########################    Integrator Inputs    ###########################

[integrator]
mode = 'Not Implemented'
[integrator.iterative_solve_params]
# Choices for iterative solve parameters within integrated solves
force_10 = false  # forces 10 iterations in solves that iterate, specify true or false (lowercase)
tol = 0.05  # percent tolerance allowed between iterations, specify a number less than 1
max_iter = 12 # max number of iterations


#                           Individual Module Inputs

###########################    Electricity Inputs    ###########################

[elec_config]

elec_regions=[7, 8, 9]  # regions to evaluate

input_path='input/electricity/cem_inputs'
output_path='output'
regional_exchange=true
capacity_expansion=true
expansion_learning_type='linear'
reserve_margin_required=true
spinning_reserve_required=true
ramping_required=true

# Select the method for projecting future electricity demand (e.g., "annual" or "enduse")
# "annual" will scale the base year electricity demand by an annual scalar value
# "end_use" will scale the base year electricity demand by hourly end use demand profiles
load_scale_mode = "end_use"

